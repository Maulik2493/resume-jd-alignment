# Resume ↔ JD Alignment Engine — Environment variables

# OpenAI API key — required for LLM-based extraction and comparison
OPENAI_API_KEY=your-api-key-here

# Model to use (default: gpt-4o)
OPENAI_MODEL=gpt-4o

# Temperature — must be 0 or near-0 per design principles
OPENAI_TEMPERATURE=0
